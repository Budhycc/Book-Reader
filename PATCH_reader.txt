Di reader.php, cari bagian RELOCATED:

    rendition.on("relocated", loc => {
        localStorage.setItem(BOOK_KEY, loc.start.cfi);
        updateProgress();
        updateBookmarkBtn();
        trackReadingTime();
    });

Tambahkan satu baris setelah updateProgress():

    rendition.on("relocated", loc => {
        localStorage.setItem(BOOK_KEY, loc.start.cfi);
        updateProgress();
        localStorage.setItem("pct-" + BOOK_URL, currentPct);   // ← tambahkan ini
        updateBookmarkBtn();
        trackReadingTime();
    });